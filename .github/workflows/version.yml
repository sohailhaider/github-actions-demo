name: CI
on: 
  push:
    branches:    
      - 'master'

jobs:
  version:
    runs-on: ubuntu-latest
    if: "!startsWith(github.event.head_commit.message, '[BETA RELEASE]')"
    steps:
      - uses: actions/checkout@v2
        with:
          token: ${{ secrets.GH_PERSONAL_ACCESS_TOKEN }}
      - run: git config --global user.name 'Sohail Haider'
      - run: git config --global user.email 'sohail.haider@autoscout24.com'
      - run: npm version prerelease --preid beta -m "[BETA RELEASE] %s"
      - run: git push && git push --tags
